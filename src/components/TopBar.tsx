import { SHOP_CONFIG } from '@/lib/config';

export default function TopBar() {
    return (
        <div className="bg-red-700 text-white py-2 px-4 text-center text-sm sm:text-base font-bold tracking-wide">
            <p>உண்மையான தரத்தின் சுவை</p>
        </div>
    );
}
